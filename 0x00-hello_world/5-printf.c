#include <stdio.h>
/**
 * main -  this is a valid main function
 *
 * Return: It always return zero (0) when compilation is successful
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
